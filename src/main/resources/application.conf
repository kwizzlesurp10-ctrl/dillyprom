# application.conf
prompt-vault {
  delta {
    # Default path for the prompts Delta table (override with --conf or env)
    table-path = "/tmp/prompt-vault/prompts"
  }
  spark {
    app-name = "PromptVault"
    log-level = "WARN"
  }
}
